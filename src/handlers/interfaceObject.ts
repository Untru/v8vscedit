import { createLeafMetaObjectHandler } from './createLeafMetaObjectHandler';

/** Интерфейсы (устаревший тип, каталог Interfaces) */
export const interfaceObjectHandler = createLeafMetaObjectHandler('Interface');
