import { emptyMetadataGroupHandler } from './emptyMetadataGroupHandler';

/** Ветка «Цвета палитры» в конфигураторе — в XML-выгрузке отдельного типа нет */
export const paletteColorHandler = emptyMetadataGroupHandler;
