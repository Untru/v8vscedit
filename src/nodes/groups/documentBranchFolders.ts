import { NodeDescriptor } from '../_types';

/** Служебная ветка «Нумераторы» внутри группы «Документы» (как в дереве конфигуратора) */
export const NumeratorsBranchDescriptor: NodeDescriptor = {
  icon: 'documentNumerator',
};

/** Служебная ветка «Последовательности» внутри группы «Документы» */
export const SequencesBranchDescriptor: NodeDescriptor = {
  icon: 'sequence',
};
