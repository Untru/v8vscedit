import { NodeDescriptor } from '../_types';

/** Хранилище настроек — каталог SettingsStorages */
export const SettingsStorageDescriptor: NodeDescriptor = {
  icon: 'attribute',
  folderName: 'SettingsStorages',
};
