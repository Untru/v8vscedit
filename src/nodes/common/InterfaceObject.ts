import { NodeDescriptor } from '../_types';

/** Интерфейс (устар.) — каталог Interfaces */
export const InterfaceObjectDescriptor: NodeDescriptor = {
  icon: 'attribute',
  folderName: 'Interfaces',
};
