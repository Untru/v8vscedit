import { NodeDescriptor } from '../_types';

/** Внешний источник данных — каталог ExternalDataSources */
export const ExternalDataSourceDescriptor: NodeDescriptor = {
  icon: 'attribute',
  folderName: 'ExternalDataSources',
};
